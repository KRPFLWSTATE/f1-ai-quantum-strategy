---
description: Project boundary and evidence rules for f1-ai-quantum-strategy
alwaysApply: true
---

Read `AGENTS.md` and `PROJECT_STATUS.md` before acting. Inspect the local evidence ledger before running anything.

Authoritative scientific protocol: `docs/protocol/` (dossier PDF, extracted text, SOURCE_HASH, traceability, protocol record). The dossier is not to be silently rewritten.

Stage 2 complete as software. Stage 3 complete as software (simulator checks). Stage 3.1 follow-up complete as software with a PARTIAL resolution-rank gate. Stage 3.2 repairs the reproduced simulator defects. Stage 3.3 evidence correction is closed. Stage 4 Final Closure: `STAGE_4_ENGINEERING: CLOSED_WITH_DOCUMENTED_LIMITATIONS`; legacy exhaustive Gate C matrix remains `PARTIAL` (withdrawn as engineering requirement). Gate E zero-headroom warning remains (`PROXY_HEADROOM: ZERO`). Do not implement QAOA, learned selectors, or any provider/QPU submission path unless a later stage prompt authorizes it. Hardware execution stays disabled. Zero additional spending. Do not materialize reserved scientific partitions. Do not invoke `formulation_gate_c_closure_check`; use `python -m f1q.formulation.stage4_closure` for the bounded gate.

Command contract: `python -m f1q doctor|status|run --plan bootstrap|run --plan development_preview|run --plan simulator_check|run --plan simulator_followup|run --plan simulator_repair|run --plan formulation_check|run --plan formulation_repair_check|resume --run-id|receipt --run-id|generator validate|plan-splits|audit|simulator validate|inspect-checkpoint|interface|diagnostic-stage3-3` plus `python -m f1q.formulation.stage4_closure`. Pushing to GitHub is a separate user instruction.
