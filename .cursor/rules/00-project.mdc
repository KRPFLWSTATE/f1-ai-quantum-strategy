---
description: Route every Agent turn through the F1 AI/quantum strategy project rules
alwaysApply: true
---

Read `AGENTS.md` and `PROJECT_STATUS.md` before acting. Inspect the local evidence ledger before running anything.

Authoritative scientific protocol: `docs/protocol/` (dossier PDF, extracted text, SOURCE_HASH, traceability, protocol record). The dossier is not to be silently rewritten.

Stage 2 complete as software. Stage 3 complete as software (simulator checks). Do not implement QUBO, learned policy, or any provider/QPU submission path unless a later stage prompt authorizes it. Hardware execution stays disabled. Zero additional spending. Do not materialize reserved scientific partitions.

Command contract: `python -m f1q doctor|status|run --plan bootstrap|run --plan development_preview|run --plan simulator_check|resume --run-id|receipt --run-id|generator validate|plan-splits|audit|simulator validate|inspect-checkpoint|interface`. Pushing to GitHub is a separate user instruction.
